//EJECUCION

Para iniciar la API puedes ejecutarla con los siguientes comandos:
   -npm run start: Para ejecutar
   -npm run dev: Para ejecutarla en modo produccion (Carga nodemon y dotenv para la lectura del puerto en el .env)